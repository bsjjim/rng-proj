<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lotterental.rng.demo.nexacro.mapper.NexacroMapper">

    <select id="selectSampleList" parameterType="nexacroVo" resultType="nexacroVo">
		SELECT SAMPLE_ID
			 , SAMPLE_NAME
			 , SAMPLE_TYPE
			 , LOC
		  FROM SAMPLE
		 WHERE SAMPLE_ID = #{sampleId}
    </select>

    <insert id="insertSample" parameterType="nexacroVo">
    	INSERT INTO SAMPLE (
    		SAMPLE_ID
			 , SAMPLE_NAME
			 , SAMPLE_TYPE
			 , LOC
			 <include refid="Common.insert_cols" />
    	) VALUES (
    		#{sampleId}
    		, #{sampleName}
    		, #{sampleType}
    		, #{loc}
    		<include refid="Common.insert_vals" />
    	)
    </insert>

    <update id="updateSample" parameterType="nexacroVo">
        UPDATE SAMPLE
           SET SAMPLE_NAME = #{sampleName}
             , SAMPLE_TYPE = #{sampleType}
             , LOC = #{loc}
             <include refid="Common.update" />
         WHERE SAMPLE_ID = #{sampleId}
    </update>

    <delete id="deleteSample" parameterType="nexacroVo">
        DELETE SAMPLE
         WHERE SAMPLE_ID = #{sampleId}
    </delete>
    
    <insert id="insert" parameterType="nexacroVo">
    	INSERT INTO SAMPLE (
    		SAMPLE_ID
			 , SAMPLE_NAME
			 , SAMPLE_TYPE
			 , LOC
			 <include refid="Common.insert_cols" />
    	) VALUES (
    		#{sampleId}
    		, #{sampleName}
    		, #{sampleType}
    		, #{loc}
    		<include refid="Common.insert_vals" />
    	)
    </insert>

    <update id="update" parameterType="nexacroVo">
        UPDATE SAMPLE
           SET SAMPLE_NAME = #{sampleName}
             , SAMPLE_TYPE = #{sampleType}
             , LOC = #{loc}
             <include refid="Common.update" />
         WHERE SAMPLE_ID = #{sampleId}
    </update>

    <delete id="delete" parameterType="nexacroVo">
        DELETE SAMPLE
         WHERE SAMPLE_ID = #{sampleId}
    </delete>
    
</mapper>
