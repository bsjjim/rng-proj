<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lotterental.rng.demo.example.mapper.ValidationMapper">

	<select id="selectBusinessInfoList" parameterType="ValidationUsedVo" resultType="ValidationUsedModel">
		SELECT 'MY_ID' AS ID
		  FROM DUAL
    </select>

</mapper>
